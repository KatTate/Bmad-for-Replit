# BMM Module Configuration
# Generated by BMAD installer
# Version: 6.0.0-Beta.8

# BMAD-specific settings (not available from Replit platform)
user_skill_level: intermediate
output_folder: _bmad-output
planning_artifacts: "{project-root}/_bmad-output/planning-artifacts"
implementation_artifacts: "{project-root}/_bmad-output/implementation-artifacts"
project_knowledge: "{project-root}/docs"

# Platform-provided values (resolved from Replit environment at runtime):
#   user_name         -> $REPLIT_USER (fallback: "User")
#   project_name      -> $REPL_SLUG (fallback: "workspace")
#   communication_language    -> derived from $LANG (fallback: "English")
#   document_output_language  -> derived from $LANG (fallback: "English")
